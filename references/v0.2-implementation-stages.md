# Agent Gateway v0.2 Implementation Stages

## Stage 1: Foundation (COMPLETE)

**Deliverable:** Action state model infrastructure

**Components:**
- v0.2 working copy at `/home/node/.openclaw/workspace/registry-v0.2/`
- `action_states` table (registry_id, action_name, state, detected_at, approved_at, verified_at)
- `action_verification_tests` table for detailed test results
- API endpoints: `/actions/detect`, `/actions/approve`, `/actions/verify`
- WordPress plugin auto-detection for WooCommerce, Bookly, Amelia, Events Calendar, CF7, WPForms, Gravity Forms
- Backwards compatibility: existing `enabled_actions` treated as manually approved

**Files changed:**
- `registry/app.py` — 119 additions for action state model
- `registry/schema_v0.2.sql` — New tables
- `wordpress-plugin/agent-gateway.php` — Detection logic + admin UI
- `docker-compose-v0.2.yml` — Port 8085 configuration

**Test URLs:**
- Registry: http://127.0.0.1:8085/health
- Search: http://127.0.0.1:8085/api/v1/search?q=test

---

## Stage 2: availability_lookup (COMPLETE)

**Deliverable:** Demo adapter + test harness

**Components:**
- `/demo/availability_adapter.py` — Mock booking system (port 8088)
- `/test-harness/` — HTML/JS test interface (port 8087)
- Rate limiting: 30 req/60s general, 10 req/60s sensitive
- Anti-scraping: Max 90-day date range, suspicious client blocking (300s)
- Demo business registered: `agw_d7c0b98802d83576`

**Adapter endpoints:**
- `GET /health` — Health check
- `GET /services` — List available services (room, suite, conference, spa)
- `GET /availability?date=YYYY-MM-DD&service=room` — Query availability
- Returns: available slots, pricing, capacity, restrictions, confirmation policy

**Files created:**
- `demo/availability_adapter.py` (567 lines)
- `demo/register_demo_business.py`
- `test-harness/index.html`
- `test-harness/app.js`
- `test-harness/server.py`

**Test URLs:**
- Adapter health: http://127.0.0.1:8088/health
- Availability: http://127.0.0.1:8088/availability?date=2026-07-01&service=room
- Test harness: http://127.0.0.1:8087/
- Business: http://127.0.0.1:8085/api/v1/business/agw_d7c0b98802d83576

---

## Stage 3: reservation_create (PENDING)

**Deliverable:** Demo reservation adapter

**Requirements:**
- POST endpoint to create reservations
- Return: reservation ID, status, expiry, payment required flag
- Clearly marked as "demo" logic
- No real reservation system integration yet

**Security considerations:**
- Mass reservation abuse prevention
- Fake reservation detection
- Inventory hoarding protection

---

## Stage 4: payment_link_generate (PENDING)

**Deliverable:** Demo payment link adapter

**Requirements:**
- Generate payment links only
- NO payment processing
- Handoff to external payment system
- Clear confirmation that payment is manual

**Security considerations:**
- Payment link spam prevention
- Phishing protection
- No card data handling

---

## Implementation Rules

1. **Preserve v0.1** — Work in v0.2 copy only
2. **Small changes** — One adapter at a time
3. **Live verification** — Every change must have testable URL
4. **Red Team gates** — Security review at each stage
5. **Rollback ready** — v0.1 backup untouched
